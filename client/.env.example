# =============================================================================
# CLIENT - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in the actual values
# NEVER commit .env.local files with real secrets to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Environment: development | production
NODE_ENV=development

# Client port
PORT=3000

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Public API base URL (used in browser)
# This is exposed to the client browser
NEXT_PUBLIC_API_BASE=http://localhost:3001/api/v1

# Internal API URL (used in server-side rendering)
# This is used for server-side API calls and is NOT exposed to the browser
INTERNAL_API_URL=http://localhost:3001/api/v1
