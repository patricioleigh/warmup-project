# =============================================================================
# SERVER - TEST ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.test and fill in the actual values (safe for tests).
# This configuration is intentionally isolated from development to prevent
# test suites from wiping your dev database/Redis DB.
# =============================================================================

NODE_ENV=test
PORT=3001

# Use a dedicated TEST database name
MONGO_URI=mongodb://root:rootpass@localhost:27017/warmupdb_test?authSource=admin

# Use a non-zero Redis DB index so tests can FLUSHDB safely
REDIS_URL=redis://localhost:6379/1
REDIS_TTL_SECONDS=3900

# Required by Config validation
JWT_SECRET=test-secret-key-for-local
MAX_ITEMS=100
MAX_RESPONSE_BYTES=262144
RATE_LIMIT_TTL_SECONDS=60
RATE_LIMIT_LIMIT=100
HN_HTTP_TIMEOUT_MS=5000

